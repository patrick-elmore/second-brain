.context/
.auth/
tmp/
index/
__pycache__/

# .NET build artifacts
**/bin/
**/obj/
publish/
*.user
*.suo
.vs/

# Claude Code runtime data (agent/skill/hook specs in .claude/ are committed)
.claude/settings.local.json
.claude/todos/
.claude/worktrees/
.claude/*.jsonl

# Project-level Claude Code instructions are personal and not part of the public repo
CLAUDE.md

# Live overrides for personal-data prompts and per-install config.
# Templates (system_prompt-template.md, aliases-template.md, sources-template.json,
# mcp_config-template.json) are committed; the live files below are bootstrapped
# from those templates on first run, then user-edited.
**/Prompts.local/
config/sources.json
config/mcp_config.json

# AliasMiner output (candidate aliases for review before promoting to aliases.md)
alias-mining/
